# Optional: credentials are normally picked up from AWS_PROFILE or
# ~/.aws/credentials. Override explicitly here if needed.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
# AWS_REGION=us-east-1
